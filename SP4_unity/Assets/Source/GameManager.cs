using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using BeardedManStudios.Forge.Networking.Unity;

public class GameManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //Random.InitState((int)System.DateTime.Now.Ticks);
        //for (int i = 0; i < PlayerManager.playerManager.m_playerCount; ++i)
        //{
        //    Vector3 randPos = new Vector3(Random.Range(0, 20), 0, Random.Range(0, 20));
        //    var newEnemy = NetworkManager.Instance.InstantiatePlayerVehicle(PlayerManager.playerManager.m_players[i].VehicleType, randPos, transform.rotation, true);
        //}
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
